apply plugin: "war"

bootWar {
    mainClassName = "ro.trc.leave.LeaveTestApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]
}
